#!/usr/bin/env python3

from os import linesep
from subprocess import PIPE, run
from typing import Sequence


def show_commits() -> Sequence[str]:
    proc = run(("git", "log", "--format", "format:%H"), stdout=PIPE)
    proc.check_returncode()
    return proc.stdout.decode().split(linesep)
